apiVersion: v1
kind: Service
metadata:
  name: {{ include "watchapi.fullname" . }}
  labels:
    {{- include "watchapi.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "watchapi.selectorLabels" . | nindent 4 }}